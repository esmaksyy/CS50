#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int numbers;
    int digits = 0;
    do
    {
        int get_long;
        numbers = get_long ("Enter numbers here: ");
        for (numbers > 10)
        {
            numbers = numbers/10
            digits++
        }

    }
    while (digits != 13 || 15 || 16);



}