#include <cs50.h>
#include <stdio.h>

int main(void)
int get_long(void)
{
    int numbers;
    do
    {
        numbers = get_long ("Enter numbers here: ");
    }
    while (numbers < 1000000000000 || numbers > );


}